# 大规模 clang-format 重排的 commit 哈希写在这里，git blame 会跳过它们。
#
# 用法（每位开发者本地执行一次，让 git blame 默认忽略下列 commit）：
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# 本次只引入 .clang-format 配置，不批量格式化存量代码，故暂无哈希。
# 后续若做一次性全量格式化，把那个 commit 的 40 位哈希逐行追加到本文件。
